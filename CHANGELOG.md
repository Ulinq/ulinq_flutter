## 1.0.0

- Production-ready SDK API (`Ulinq.initialize`, `resolve`, `claimInstallAttribution`, `onLink`, `trackOpen`).
- Added Android Play Install Referrer integration via plugin channel.
- Added iOS pending install token retrieval fallback via plugin channel.
- Added typed exceptions, retry logic, and rate-limit handling.
- Added example app and unit tests.
- Added conversion tracking API (`trackConversion`) mapped to `/sdk/v1/events/conversion`.
