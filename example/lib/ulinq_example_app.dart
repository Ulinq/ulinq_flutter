import 'package:flutter/material.dart';

import 'ulinq_example_page.dart';

class UlinqExampleApp extends StatelessWidget {
  const UlinqExampleApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(home: UlinqExamplePage());
  }
}
